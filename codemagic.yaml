workflows:
  friday_android:
    name: FRIDAY v1.1 APK
    max_build_duration: 60

    environment:
      java: 17

    scripts:
      - name: Build FRIDAY APK
        script: |
          cd android
          gradle --no-daemon assembleDebug

    artifacts:
      - android/app/build/outputs/apk/debug/*.apk
